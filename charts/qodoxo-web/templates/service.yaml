apiVersion: v1
kind: Service
metadata:
  name: {{ include "qodoxo-web.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "qodoxo-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "qodoxo-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
